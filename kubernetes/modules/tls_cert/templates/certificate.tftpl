apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  secretName: ${secret_name}
  issuerRef:
    kind: ClusterIssuer
    name: ${issuer_name}
  dnsNames: 
    %{ for domain_name in domain_names ~}
    - ${domain_name}
    %{ endfor ~}
